RSpec.describe Selenium::Tutorial::Ruby do
  it "has a version number" do
    expect(Selenium::Tutorial::Ruby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
